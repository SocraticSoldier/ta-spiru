# Jarvis's brain — required for the assistant, Malti and the daily brief
ANTHROPIC_API_KEY="sk-ant-..."

# Optional. Unlocks the /vault section. Leave unset and the vault stays closed
# rather than falling open. Never commit a real value.
JARVIS_VAULT_PASSWORD=""
