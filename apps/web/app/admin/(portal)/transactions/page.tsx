import type { JSX } from 'react';
import { Placeholder } from '@/components/admin/placeholder';

const TransactionsPage = (): JSX.Element => (
  <Placeholder
    title="Transactions"
    phase="Phase 1.1"
    description="Trust Payments ledger browser: settlements, declines, refunds and revenue splits across the four business lines."
  />
);

export default TransactionsPage;
