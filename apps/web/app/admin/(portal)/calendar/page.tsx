import type { JSX } from 'react';
import { Placeholder } from '@/components/admin/placeholder';

const CalendarPage = (): JSX.Element => (
  <Placeholder
    title="Calendar"
    phase="Phase 3"
    description="Master daily calendar across barber chairs and wash bays, with combo bookings and the WebSocket-powered virtual queue."
  />
);

export default CalendarPage;
